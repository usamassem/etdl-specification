asyncapi: "3.0.0"
info:
  title: Payments
  version: "1.0.0"
channels: {}
components:
  messages:
    PaymentRequested:
      name: PaymentRequested
      payload:
        type: object
        properties:
          ok: { type: boolean }
